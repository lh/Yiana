{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear",
        "hooks": [
          {
            "type": "command",
            "command": "cd /Users/rose/Code/Yiana && echo '=== Git Status ===' && git status --short && echo '=== Unstaged Changes ===' && git diff --stat"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "TaskUpdate",
        "hooks": [
          {
            "type": "command",
            "command": "INPUT=$(cat); if echo \"$INPUT\" | grep -q 'completed'; then echo 'Build check triggered -- task marked complete'; xcodebuild -project Yiana/Yiana.xcodeproj -scheme Yiana -destination 'generic/platform=iOS' -quiet build 2>&1 | tail -5; xcodebuild -project Yiana/Yiana.xcodeproj -scheme Yiana -destination 'platform=macOS' -quiet build 2>&1 | tail -5; fi",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
